# Required for React Native / Expo (when added): Metro doesn't follow symlinks
# node-linker=hoisted  ← uncomment when adding the Expo mobile app

# Strict pnpm settings for now
shamefully-hoist=false
